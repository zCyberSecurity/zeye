- name: Nginx
  category: web-server
  tags: [nginx, web]
  version_pattern: "nginx/([\\d.]+)"
  min_weight: 50
  matches:
    - field: header.server
      type: regex
      pattern: "nginx"
      weight: 100
    - field: body
      type: keyword
      pattern: "nginx"
      weight: 30

- name: Apache
  category: web-server
  tags: [apache, web]
  version_pattern: "Apache/([\\d.]+)"
  min_weight: 50
  matches:
    - field: header.server
      type: regex
      pattern: "Apache"
      weight: 100
    - field: body
      type: keyword
      pattern: "Apache"
      weight: 30

- name: IIS
  category: web-server
  tags: [iis, windows, web]
  version_pattern: "IIS/([\\d.]+)"
  min_weight: 50
  matches:
    - field: header.server
      type: regex
      pattern: "Microsoft-IIS"
      weight: 100

- name: Tomcat
  category: web-server
  tags: [tomcat, java, web]
  min_weight: 50
  matches:
    - field: header.server
      type: keyword
      pattern: "Apache-Coyote"
      weight: 80
    - field: body
      type: keyword
      pattern: "Apache Tomcat"
      weight: 80
    - field: body
      type: keyword
      pattern: "tomcat"
      weight: 40

- name: WordPress
  category: cms
  tags: [wordpress, cms, php]
  min_weight: 60
  matches:
    - field: body
      type: keyword
      pattern: "wp-content"
      weight: 80
    - field: body
      type: keyword
      pattern: "wp-includes"
      weight: 80
    - field: header.x-powered-by
      type: keyword
      pattern: "WordPress"
      weight: 60

- name: Drupal
  category: cms
  tags: [drupal, cms, php]
  min_weight: 60
  matches:
    - field: body
      type: regex
      pattern: "Drupal"
      weight: 80
    - field: header.x-generator
      type: keyword
      pattern: "Drupal"
      weight: 100

- name: Joomla
  category: cms
  tags: [joomla, cms, php]
  min_weight: 60
  matches:
    - field: body
      type: keyword
      pattern: "/components/com_"
      weight: 80
    - field: body
      type: keyword
      pattern: "joomla"
      weight: 60

- name: Laravel
  category: framework
  tags: [laravel, php, framework]
  min_weight: 60
  matches:
    - field: header.set-cookie
      type: keyword
      pattern: "laravel_session"
      weight: 100
    - field: body
      type: keyword
      pattern: "laravel"
      weight: 40

- name: Spring Boot
  category: framework
  tags: [spring, java, framework]
  min_weight: 60
  matches:
    - field: header.x-application-context
      type: keyword
      pattern: ""
      weight: 80
    - field: body
      type: keyword
      pattern: "Whitelabel Error Page"
      weight: 100
    - field: header.server
      type: keyword
      pattern: "Spring"
      weight: 60

- name: Django
  category: framework
  tags: [django, python, framework]
  min_weight: 60
  matches:
    - field: header.server
      type: keyword
      pattern: "WSGIServer"
      weight: 80
    - field: body
      type: keyword
      pattern: "Django"
      weight: 60
    - field: header.x-frame-options
      type: equals
      value: "SAMEORIGIN"
      weight: 20

- name: PHP
  category: language
  tags: [php]
  version_pattern: "PHP/([\\d.]+)"
  min_weight: 50
  matches:
    - field: header.x-powered-by
      type: regex
      pattern: "PHP"
      weight: 100
    - field: header.server
      type: regex
      pattern: "PHP"
      weight: 80

- name: Shiro
  category: security
  tags: [shiro, java, auth]
  min_weight: 80
  matches:
    - field: header.set-cookie
      type: regex
      pattern: "rememberMe=deleteMe"
      weight: 100

- name: Kibana
  category: monitoring
  tags: [kibana, elasticsearch, elk]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "Kibana"
      weight: 100
    - field: body
      type: keyword
      pattern: "kibana"
      weight: 60

- name: Grafana
  category: monitoring
  tags: [grafana, monitoring]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "Grafana"
      weight: 100
    - field: body
      type: keyword
      pattern: "grafana"
      weight: 60

- name: Jenkins
  category: ci-cd
  tags: [jenkins, devops]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "Jenkins"
      weight: 100
    - field: header.x-jenkins
      type: keyword
      pattern: ""
      weight: 100

- name: GitLab
  category: devops
  tags: [gitlab, git, devops]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "GitLab"
      weight: 100
    - field: body
      type: keyword
      pattern: "gitlab"
      weight: 60

- name: Prometheus
  category: monitoring
  tags: [prometheus, monitoring]
  min_weight: 80
  matches:
    - field: title
      type: equals
      value: "Prometheus Time Series Collection and Processing Server"
      weight: 100
    - field: body
      type: keyword
      pattern: "prometheus"
      weight: 40

- name: MinIO
  category: storage
  tags: [minio, s3, storage]
  min_weight: 60
  matches:
    - field: header.server
      type: keyword
      pattern: "MinIO"
      weight: 100
    - field: title
      type: keyword
      pattern: "MinIO"
      weight: 80

- name: Phpmyadmin
  category: database
  tags: [phpmyadmin, mysql, admin]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "phpMyAdmin"
      weight: 100
    - field: body
      type: keyword
      pattern: "phpmyadmin"
      weight: 80

- name: Weblogic
  category: web-server
  tags: [weblogic, oracle, java]
  min_weight: 60
  matches:
    - field: header.server
      type: keyword
      pattern: "WebLogic"
      weight: 100
    - field: body
      type: keyword
      pattern: "WebLogic"
      weight: 60

- name: Hikvision
  category: iot
  tags: [hikvision, camera, iot]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "HIKVISION"
      weight: 100
    - field: body
      type: keyword
      pattern: "hikvision"
      weight: 80

- name: Dahua
  category: iot
  tags: [dahua, camera, iot]
  min_weight: 60
  matches:
    - field: title
      type: keyword
      pattern: "Web Service"
      weight: 40
    - field: body
      type: keyword
      pattern: "dahua"
      weight: 80

- name: Router-Admin
  category: network
  tags: [router, admin, network]
  min_weight: 40
  matches:
    - field: title
      type: keyword
      pattern: "router"
      weight: 60
    - field: body
      type: keyword
      pattern: "admin"
      weight: 20

# ── SSH ───────────────────────────────────────────────────────────────────────

- name: OpenSSH
  category: ssh-server
  tags: [ssh, openssh, remote]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "OpenSSH_([\d.p]+)"
      weight: 100

- name: Dropbear
  category: ssh-server
  tags: [ssh, dropbear, embedded]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "dropbear_([\d.]+)"
      weight: 100

- name: Bitvise SSH Server
  category: ssh-server
  tags: [ssh, bitvise, windows]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Bitvise"
      weight: 100

- name: libssh
  category: ssh-server
  tags: [ssh, libssh]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "libssh_([\d.]+)"
      weight: 100

- name: Cisco SSH
  category: ssh-server
  tags: [ssh, cisco, network]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Cisco"
      weight: 100

# ── FTP ───────────────────────────────────────────────────────────────────────

- name: vsftpd
  category: ftp-server
  tags: [ftp, vsftpd, linux]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "vsFTPd ([\d.]+)"
      weight: 100

- name: ProFTPD
  category: ftp-server
  tags: [ftp, proftpd, linux]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "ProFTPD ([\d.]+)"
      weight: 100

- name: Pure-FTPd
  category: ftp-server
  tags: [ftp, pure-ftpd, linux]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Pure-FTPd"
      weight: 100

- name: FileZilla Server
  category: ftp-server
  tags: [ftp, filezilla, windows]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "FileZilla Server ([\d.]+)"
      weight: 100

- name: Microsoft FTP Service
  category: ftp-server
  tags: [ftp, microsoft, windows, iis]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Microsoft FTP Service"
      weight: 100

# ── SMTP ──────────────────────────────────────────────────────────────────────

- name: Postfix
  category: smtp-server
  tags: [smtp, postfix, linux]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Postfix"
      weight: 100

- name: Exim
  category: smtp-server
  tags: [smtp, exim, linux]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "Exim ([\d.]+)"
      weight: 100

- name: Sendmail
  category: smtp-server
  tags: [smtp, sendmail, linux]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Sendmail"
      weight: 100

- name: Microsoft Exchange SMTP
  category: smtp-server
  tags: [smtp, exchange, microsoft, windows]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Microsoft ESMTP MAIL Service"
      weight: 100

- name: Dovecot SMTP
  category: smtp-server
  tags: [smtp, dovecot, linux]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Dovecot"
      weight: 100
    - field: app_proto
      type: equals
      value: "smtp"
      weight: 50

# ── IMAP ──────────────────────────────────────────────────────────────────────

- name: Dovecot IMAP
  category: imap-server
  tags: [imap, dovecot, linux]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Dovecot"
      weight: 100
    - field: app_proto
      type: keyword
      pattern: "imap"
      weight: 50

- name: Cyrus IMAP
  category: imap-server
  tags: [imap, cyrus]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Cyrus IMAP"
      weight: 100

- name: Microsoft Exchange IMAP
  category: imap-server
  tags: [imap, exchange, microsoft, windows]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Microsoft Exchange IMAP4"
      weight: 100

- name: Courier IMAP
  category: imap-server
  tags: [imap, courier]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Courier-IMAP"
      weight: 100

# ── POP3 ──────────────────────────────────────────────────────────────────────

- name: Dovecot POP3
  category: pop3-server
  tags: [pop3, dovecot, linux]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Dovecot"
      weight: 100
    - field: app_proto
      type: keyword
      pattern: "pop3"
      weight: 50

- name: Microsoft Exchange POP3
  category: pop3-server
  tags: [pop3, exchange, microsoft, windows]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Microsoft Exchange POP3"
      weight: 100

- name: Courier POP3
  category: pop3-server
  tags: [pop3, courier]
  min_weight: 100
  matches:
    - field: banner
      type: keyword
      pattern: "Courier"
      weight: 80
    - field: app_proto
      type: equals
      value: "pop3"
      weight: 50

# ── MySQL / MariaDB / Percona ─────────────────────────────────────────────────

- name: MariaDB
  category: database
  tags: [mysql, mariadb, database]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "([\d.]+-MariaDB)"
      weight: 100
    - field: banner
      type: keyword
      pattern: "MariaDB"
      weight: 100

- name: Percona Server
  category: database
  tags: [mysql, percona, database]
  min_weight: 100
  matches:
    - field: banner
      type: regex
      pattern: "([\d.]+-Percona)"
      weight: 100
    - field: banner
      type: keyword
      pattern: "Percona"
      weight: 100

- name: MySQL
  category: database
  tags: [mysql, database]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "mysql"
      weight: 80
    - field: banner
      type: keyword
      pattern: "MySQL"
      weight: 80

# ── Redis ─────────────────────────────────────────────────────────────────────

- name: Redis
  category: database
  tags: [redis, cache, database]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "redis"
      weight: 80
    - field: banner
      type: keyword
      pattern: "Redis"
      weight: 80

# ── MongoDB ───────────────────────────────────────────────────────────────────

- name: MongoDB
  category: database
  tags: [mongodb, nosql, database]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "mongodb"
      weight: 80
    - field: banner
      type: keyword
      pattern: "MongoDB"
      weight: 80

# ── PostgreSQL ────────────────────────────────────────────────────────────────

- name: PostgreSQL
  category: database
  tags: [postgresql, database]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "postgresql"
      weight: 80
    - field: banner
      type: keyword
      pattern: "PostgreSQL"
      weight: 80

# ── Memcached ─────────────────────────────────────────────────────────────────

- name: Memcached
  category: database
  tags: [memcached, cache]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "memcached"
      weight: 80
    - field: banner
      type: regex
      pattern: "VERSION ([\d.]+)"
      weight: 80

# ── Oracle ────────────────────────────────────────────────────────────────────

- name: Oracle Database
  category: database
  tags: [oracle, database]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "oracle"
      weight: 80
    - field: banner
      type: keyword
      pattern: "Oracle"
      weight: 80

# ── SMB ───────────────────────────────────────────────────────────────────────

- name: Samba
  category: smb-server
  tags: [smb, samba, linux]
  min_weight: 80
  matches:
    - field: app_proto
      type: keyword
      pattern: "smb"
      weight: 60
    - field: banner
      type: keyword
      pattern: "Samba"
      weight: 100

- name: Windows SMB
  category: smb-server
  tags: [smb, smb2, windows]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "smb2"
      weight: 100

# ── MQTT ──────────────────────────────────────────────────────────────────────

- name: Eclipse Mosquitto
  category: mqtt-broker
  tags: [mqtt, mosquitto, iot]
  min_weight: 80
  matches:
    - field: app_proto
      type: keyword
      pattern: "mqtt"
      weight: 60
    - field: banner
      type: keyword
      pattern: "Mosquitto"
      weight: 100

- name: EMQX
  category: mqtt-broker
  tags: [mqtt, emqx, iot]
  min_weight: 80
  matches:
    - field: app_proto
      type: keyword
      pattern: "mqtt"
      weight: 60
    - field: banner
      type: keyword
      pattern: "EMQ"
      weight: 100

- name: MQTT Broker
  category: mqtt-broker
  tags: [mqtt, iot]
  min_weight: 80
  matches:
    - field: app_proto
      type: keyword
      pattern: "mqtt"
      weight: 100

# ── Industrial ────────────────────────────────────────────────────────────────

- name: Modbus TCP
  category: industrial
  tags: [modbus, ics, scada]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "modbus"
      weight: 100

- name: DNP3
  category: industrial
  tags: [dnp3, ics, scada]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "dnp3"
      weight: 100

# ── Other services ────────────────────────────────────────────────────────────

- name: Telnet
  category: service
  tags: [telnet, remote]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "telnet"
      weight: 100

- name: SOCKS5 Proxy
  category: proxy
  tags: [socks5, proxy]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "socks5"
      weight: 100

- name: NTP
  category: service
  tags: [ntp, time]
  min_weight: 80
  matches:
    - field: app_proto
      type: equals
      value: "ntp"
      weight: 100
